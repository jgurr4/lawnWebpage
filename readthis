connect by using localhost:8081
This project is run using Node.js, MariaDB Connector/Database, and Docker Compose.

Whenever you make changes or updates to this app src files you must rebuild the docker container.
cd /src
docker build -t jaredsdev/nodejsapp .
docker-compose up -d

mysql by default uses port 3306
in docker-compose.yml and mariadbscript.js I have 'port = 8081'.

In config.js I have credentials for mariadbscript.js
